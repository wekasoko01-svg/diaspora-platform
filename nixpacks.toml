[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@9.15.9 && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["cd packages/db && pnpm generate && cd ../../apps/api && pnpm build"]

[start]
cmd = "cd apps/api && node dist/server.js"
