{
  "framework": "nextjs",
  "buildCommand": "cd .. && pnpm build --filter=@diaspora/web",
  "outputDirectory": ".next",
  "installCommand": "pnpm install --frozen-lockfile",
  "env": {
    "NEXT_PUBLIC_API_URL": "@next_public_api_url"
  }
}
